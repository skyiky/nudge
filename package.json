{
  "name": "nudge",
  "version": "0.1.0",
  "type": "module",
  "description": "Nudge — AI-persistent nudges embedded in your development workflow",
  "scripts": {
    "mcp": "bun run src/mcp/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9"
  }
}
